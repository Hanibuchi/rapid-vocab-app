# 英単語学習アプリ 要件定義・仕様書

## 1. アプリケーション概要と成果物形式
- **形式**: HTML / CSS / JavaScript をすべて1つにまとめた単一のファイル（`index.html`）。
- **利用環境**: 利用者がダウンロードし、`index.html` をダブルクリックするだけでブラウザ上で動作する。外部サーバーを介さず完結する構成とする。
- **データベース**: ローカルストレージとして `IndexedDB` を使用する。

---

## 2. データ仕様・DB構造 (IndexedDB)
- **管理キー**: 「ファイル名 + 単語」の組み合わせで一意に管理する。
  - 同一単語が複数のファイルに存在する場合、それぞれ別個のデータとして扱う。
  - 同一ファイル内に同じ単語が複数記述されていた場合、最初の1件目のみを登録・読み込み対象とする。
- **記録項目**:
  - 印（マーク）の状態
- **設定データの永続化**:
  - 設定画面で選択した各項目・トグルの状態はDBに保存し、次回起動時や設定画面表示時に前回の状態を復元する。
- **データ更新タイミング**:
  - 出題途中で「設定へ戻る」ボタンを押下して中断した場合でも、そこまでに解答した問題の成否および印の変更内容はDBへ反映・保存する。

---

## 3. ファイル管理仕様
- **ファイル形式**: テキストファイル（CSV形式：`英単語,日本語訳`）。
- **アップロード**:
  - 設定画面のファイル選択ダイアログからアップロードする。
  - 同名のファイルは追加できない（重複不可）。
- **削除**:
  - アップロード済みファイルの削除が可能。
  - 削除実行時には確認の警告ダイアログを表示する。
- **説明画面**:
  - 設定画面からモーダル等で説明画面を開くことができる。
  - ファイルのアップロード方法およびファイルの形式（CSV形式：英単語,日本語訳）についての説明を記載する。

---

## 4. 画面・機能仕様

### 4.1. 設定画面（初期画面）
- **単語セット選択**:
  - アップロードされたファイル一覧を表示する。各ファイル名を表示し、ファイル単位で出題対象とするかをトグルで切り替え可能。
  - 多数のファイルが存在しても閲覧・操作できるよう、一覧部分はスクロール可能とする。
- **モード選択**:
  - 「英語 → 日本語」モード
  - 「日本語 → 英語」モード
- **設定トグル**:
  - 【共通】時間制限で自動で次へ進むか（ON / OFF）
  - 【共通】印のついた単語だけ出題するか（ON / OFF）
  - 【英語 → 日本語限定】綴りを表示するか（ON / OFF。OFFの場合は綴りを非表示にし、音声のみで解答させる）
  - 【日本語 → 英語】固有のトグルなし
- **開始条件**:
  - 選択中のファイルおよび設定により、出題対象となる単語数が0問の場合は開始ボタンを押せない（開始不可）とする。

---

### 4.2. 出題画面
- **出題順**: 対象単語からランダムに出題。
- **画面表示要素**:
  - 現在のファイル名
  - 進捗表示（例: `12 / 150問`）
  - 出題テキスト（英語→日本語で綴り非表示の場合は非表示）
  - 音声再生ボタン（英語→日本語限定。音声再生中は押下不可とする）
  - 4択の選択肢ボタン
  - 印をつける/外すトグル（ユーザーが手動で切り替え可能）
  - 「設定へ戻る」ボタン（途中で中断して設定画面に戻る。それまでの成否・印の状態はDBに保存）
- **4択選択肢の生成ロジック**:
  - 正答1つ ＋ 間違い選択肢3つ。
  - 間違い選択肢は、今回出題対象となっている単語群の中からランダムに抽出する。
  - 出題対象の単語が4つ未満の場合は、足りない分を内部で定義した固定の単語を間違い選択肢として表示する。
  - 日本語訳の文字列が表示領域を超えて長すぎる場合は、末尾を `...` で省略する。
- **時間制限と判定**:
  - 各問題の制限時間は3秒。
  - 「時間制限で自動で次へ進む」がONの場合: 制限時間経過した時点で不正解扱いとなり、自動で次の単語へ遷移する。
  - 「時間制限で自動で次へ進む」がOFFの場合: 制限時間経過しても自動遷移せず、正答を選択できれば正解として扱う。
- **解答速度演出（正答時）**:
  - 解答までの残り時間割合に応じて、表示される英単語（評価テキスト）が変化する:
    - 残り80%以上: `Incredible!`
    - 残り60%以上 80%未満: `Awesome!`
    - 残り40%以上 60%未満: `Cool!`
    - 残り20%以上 40%未満: `Nice!`
    - 残り20%未満: `Phew!`
- **解答後の遷移処理**:
  - いずれかの選択肢を選択すると、成否・対応するSE・正解の回答を表示する。
  - 間違えた単語には自動で「印」が付与される。
  - 表示待機時間: 正答時は1秒間、誤答時は2秒間。待機後に次の単語を出題する。

---

### 4.3. 振り返り画面
- **遷移演出**:
  - 全単語の回答が終了した際、正答率に応じたモチベーション向上テキストを表示した後に振り返り画面へ遷移する。
    - 正答率 100%: `Perfect!`
    - 正答率 80%以上: `Great Job!`
    - 80%未満: 特になし（演出テキストは非表示）
- **画面表示・機能**:
  - 表形式で今回の結果一覧を表示（項目: 単語、成否、印）。
  - 表内の「印」は、その場で付け外し（ON/OFF切り替え）が可能。
  - **ボタン**:
    - 設定そのままでリトライ
    - その回で間違えた問題だけリトライ
    - 設定画面に戻る

---

## 5. 音声・効果音（SE）仕様
- **発音機能**:
  - 単語の発音音声はなんらかの方法（Web Speech API SpeechSynthesis等）を用いて自動生成・再生する。
  - 英語→日本語モードでは、出題開始時に自動で発音が再生される。
- **効果音（SE）**:
  - 正解時、不正解時などの各所で対応するSEを鳴らす。
  - SEは外部音声ファイルに依存せず、なんらかの方法（Web Audio API等）を用いて自動生成・再生する。